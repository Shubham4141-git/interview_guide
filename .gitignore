# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# ── Python bytecode / caches
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg-info/
*.egg

# ── Virtual environments
.venv/
venv/

# ── Local env + secrets (use HF Spaces Secrets instead)
.env
.env.*

# ── Local databases / artifacts
*.db
*.sqlite
*.sqlite3

# ── OS / Editor junk
.DS_Store
Thumbs.db
.vscode/
.idea/

# ── Streamlit local config (keep secrets local; Spaces uses HF Secrets)
.streamlit/secrets.toml

# ── Tests / caches (if you add tests later)
.pytest_cache/
.coverage
htmlcov/